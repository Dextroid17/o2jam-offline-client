diff --git a/src/SFML/Graphics/CMakeLists.txt b/src/SFML/Graphics/CMakeLists.txt
index 40bd4e97..a5dc1416 100644
--- a/src/SFML/Graphics/CMakeLists.txt
+++ b/src/SFML/Graphics/CMakeLists.txt
@@ -519,8 +519,15 @@ else()
     target_compile_options(freetype PRIVATE -w)
     target_compile_options(harfbuzz PRIVATE -w)
 
-    add_library(Freetype::Freetype ALIAS freetype)
-    add_library(HarfBuzz::HarfBuzz ALIAS harfbuzz)
+    # Guarded: Genode's find_package(Freetype/HarfBuzz) may have already created
+    # these imported targets from the system libraries, which makes CMake abort
+    # with 'another target with the same name already exists'.
+    if(NOT TARGET Freetype::Freetype)
+        add_library(Freetype::Freetype ALIAS freetype)
+    endif()
+    if(NOT TARGET HarfBuzz::HarfBuzz)
+        add_library(HarfBuzz::HarfBuzz ALIAS harfbuzz)
+    endif()
 endif()
 
 target_link_libraries(sfml-graphics PRIVATE Freetype::Freetype HarfBuzz::HarfBuzz)
